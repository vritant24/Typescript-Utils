{
  "private": "true",
  "workspaces": [
    "async-action-pump",
    "event-utils"
  ],
  "scripts": {
    "lint": "eslint --ext .js,.ts .",
    "build": "yarn --cwd ./event-utils build &&yarn --cwd ./async-action-pump build"
  }
}
