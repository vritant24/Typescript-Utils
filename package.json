{
  "private": "true",
  "workspaces": [
    "async-action-pump",
    "event-utils"
  ],
  "scripts": {
    "lint": "eslint --ext .js,.ts .",
    "build": "yarn --cwd async-action-pump build && yarn --cwd event-utils build",
    "watch": "tsc -w"
  }
}
